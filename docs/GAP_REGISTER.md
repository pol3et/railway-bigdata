# Gap Register

Date: 2026-06-24

Source: `docs/MERGE_STRATEGY.md`, `docs/TEST_FIRST_INTEGRATION_PLAN.md`, and characterization-test results.

Status values:

- `open`: known gap, not fixed yet.
- `in_progress`: file changes exist but verification is not complete.
- `closed`: verification command passed and evidence is recorded.

## Gaps

| ID | Status | Owner | Sync Point | Evidence | Expected Behavior | Closure Criteria | Verification Command |
|---|---|---|---|---|---|---|---|
| GAP-001 | closed | Workstream A - DevEx / Repo Owner | S1 package/import contract | No dependency manifest existed under `bigdata/course_proj`; earlier imports failed on missing `pandas` and `s3fs`. | Classmates can install runtime and test dependencies from the project root. | Closed 2026-06-21: `pyproject.toml` exists and editable install passed. | `python -m pip install -e ".[test]"` |
| GAP-002 | closed | Workstream A - DevEx / Repo Owner | S1 package/import contract | Current code used competing import roots: `bronze`, `silver`, `gold`, and intended `railway_lakehouse`. | One stable import root exists after migration: `railway_lakehouse`. | Closed 2026-06-21: code moved to `src/railway_lakehouse` and package imports passed. | `python -c "import railway_lakehouse; import railway_lakehouse.pipeline"` |
| GAP-003 | closed | Workstream C - Bronze Owner | S1 package/import contract | Operational Bronze code was under `bronze/bronze`; new source adapters were under `railway_lakehouse/bronze/sources`. | One Bronze package owns raw landing and all source adapters. | Closed 2026-06-21: Bronze files and source adapters live under `src/railway_lakehouse/bronze`. | `python -m pytest -q -m unit` |
| GAP-004 | closed | Workstream H - Live Ops / Real Data Owner | S3 local fixture E2E | Closed 2026-06-22: `src/railway_lakehouse/pipeline.py` reads deterministic local Bronze fixtures and fixture evidence exists at `output/evidence/fixture-e2e/railway_ml.parquet`. | Pipeline can read deterministic Bronze fixtures before live collectors run. | `_read_bronze_eurostat` and `_read_bronze_news` have tested fixture-backed implementations. | `python -m pytest -q -m integration` |
| GAP-005 | open | Workstream C - Bronze Owner | S2 data contracts | `src/railway_lakehouse/bronze/run.py` schedules Eurostat, World Bank, GDELT, and RSS only. `parser/ksh-stadat` and `parser/uic-refresh` Bronze source work is done and live-validated, but KSH and UIC are still not scheduled. | KSH, Statistik Austria, UIC, and historical GDELT adapters are wired without changing raw landing semantics. | Scheduler imports and runs every configured source through the same `RawLander` contract. | `python -m pytest -q -m unit` |
| GAP-006 | open | Workstream D - Silver Stats Owner / Workstream E - Silver News Owner | S3 local fixture E2E | Update 2026-06-23: PR #10 and PR #9 are merged into local `main`. Silver Stats now reads Bronze World Bank JSON + Eurostat TSV(.gz) fixture bytes into `StatFact` rows, persists Parquet, preserves provenance, exposes unmapped labels, and normalizes World Bank `AUT` to project geo `AT`. Silver News now parses RSS XML and GDELT ArtList JSON into `ArticleRecord` rows with stable IDs, and local `_read_bronze_news()` accepts RSS XML fixtures. Remaining GAP-006 work: KSH XLSX -> `StatFact`, Statistik Austria `.ods` -> `StatFact`, UIC PDF/subscribed export -> `StatFact`, and persisted Silver news output/extraction-failure accounting. Update 2026-06-23 (PR `silver/persist-outputs`): the local Silver persistence contract is frozen in `docs/DATA_CONTRACTS.md` and implemented in `silver/persist.py` — `StatFact`/successful `NewsFeature` rows are written as one replaceable snapshot per `silver/<domain>/<table>/ingest_date=.../*.parquet` path with `load_stats`/`load_news` readers; an integration test persists fixture stats, reloads from disk, and feeds Gold (proving the GAP-006->GAP-007 library hand-off). MinIO/s3fs Silver persistence and news extraction-failure accounting remain separate follow-up work. Update 2026-06-24 (`silver/stats-ksh-xlsx-reader`): KSH XLSX Bronze files are now parsed into the Silver `StatFact` contract through a deterministic `openpyxl` reader registered as `ksh`. Verification: `tests/test_silver_stats_ksh.py` passed with 4 tests, and the full suite passed with 130 passed, 1 skipped. Remaining GAP-006 extra parser work: Statistik Austria `.ods` -> `StatFact` and UIC PDF/subscribed export -> `StatFact`. | Silver reads Bronze fixtures/storage and writes auditable Silver outputs. Remaining national/statistical files become validated `StatFact` rows without LLM numeric rewriting; news extraction failures are visible. | Stats and news fixture inputs produce persisted Silver rows with unmapped labels/extraction failures visible; KSH/Statistik Austria/UIC fixture parser tests pass. | `python -m pytest -q -m integration` |
| GAP-007 | closed | Workstream F - Gold Owner | S3 local fixture E2E | Closed 2026-06-24: `gold/run.py` now loads persisted local Silver Parquet via `persist.load_stats/load_news`, forwards optional `--ingest-date`, writes Gold under an `output/` default, and records counts with the shared Gold counts writer. Local CLI smoke from a tmp Silver tree wrote a 4x4 Gold parquet and counts with AT/HU and `rail_passenger_km`. | Gold can load persisted Silver outputs and write analysis-ready Parquet with row/column evidence. | Closed: fixture Silver inputs produce Gold Parquet and recorded row/column counts through the CLI, with no in-memory Silver handoff, no network, no MinIO, and no Ollama. | `python -m pytest -q -m integration` |
| GAP-008 | closed | Workstream B - QA / Gap Register Owner | S1 package/import contract | No `tests/` directory existed under `bigdata/course_proj`. | Deterministic tests cover current Bronze/Silver/Gold behavior and live checks are opt-in. | Closed 2026-06-21: unit tests exist, markers are registered, and expected failure references GAP-004. | `python -m pytest -q` |
| GAP-009 | closed | Workstream G - Spark / Big Data Owner | S5 Spark evidence | Closed 2026-06-24: `src/railway_lakehouse/spark_jobs/coverage.py` now imports and runs. The job reads the real Gold Parquet at `output/evidence/inventory-live-2026-06-23/railway_ml.parquet` and writes `output/evidence/spark/manifest.json` plus Spark output directory `output/evidence/spark/coverage_by_geo_year/`. Manifest records Spark 4.1.2, JDK 21.0.11, input 2,968 rows x 4 columns, output 2,968 rows x 5 columns, one part-file, `_SUCCESS`, duration, and UTC timestamps. | Spark job reads Gold/Silver data and writes evidence outputs. | Spark command records version, row counts, generated files, and warnings/failures. Closed by committed evidence under `output/evidence/spark/`; missing input and 0-row input fail loudly. | `python -m railway_lakehouse.spark_jobs.coverage --input output/evidence/inventory-live-2026-06-23/railway_ml.parquet --out output/evidence/spark/` |
| GAP-010 | in_progress | Workstream H - Live Ops / Real Data Owner | S4 live Bronze/Silver/Gold run | Bounded RSS Bronze evidence exists at `output/evidence/live-bronze/manifest.json`; current KSH evidence exists at `output/evidence/ksh-live-check-2026-06-22-current/manifest.json`; UIC public PDF evidence exists at `output/evidence/uic-live-check-2026-06-22/manifest.json`; parser PR verification added bounded Eurostat and World Bank live probes. Update 2026-06-23 (PR `bronze/local-stats-landing`): bounded local Eurostat + World Bank raw Bronze can be regenerated and fed through the pipeline to `output/evidence/first-real-gold-local-stats-v2/railway_ml.parquet` with `counts.json` recording 2,139 rows x 3 columns, AT/HU present, and World Bank `rail_network_length_km` as the current real Gold feature. Update 2026-06-23 (PR `infra/minio-storage`): local MinIO object storage has Docker Compose setup, `bronze`/`silver`/`gold` bucket smoke evidence at `output/evidence/minio-smoke/manifest.json`, and deterministic guard coverage in `tests/test_infra_minio.py`; full persisted Bronze/Silver/Gold through MinIO/Ollama/news/Spark remains incomplete. Update 2026-06-24: the live MinIO path is now proven on this machine (`docker compose up -d` + `scripts/minio_smoke.py` round-trip passed), and a live World Bank Bronze→Silver→Gold run produced a richer real Gold (2,968×4). GAP-012 is closed by the corrected regen recipe and missing-root guard; see GAP-013 for the remaining live MinIO stats branch dropping World Bank. Full MinIO/Ollama/news/Spark E2E still open. | Bounded fixture and live runs generate evidence under `output/evidence/`. | Commands, logs, row counts, and generated paths are recorded. | `python -m pytest -q -m integration` |
| GAP-011 | closed | Workstream I - Report / Presentation Owner | S5 Spark evidence | Closed 2026-06-24: report and presentation drafts now exist at `output/report/REPORT.md` and `output/presentation/PRESENTATION.md`. The drafts cite committed evidence JSON under `output/evidence/`, include an Evidence Index claim->path->JSON-key table, and are guarded by `tests/test_report_evidence_links.py`. | Report and presentation use only verified outputs and documented gaps. | Closed: drafts link headline data claims to evidence artifacts; the checker asserts cited paths exist and curated headline values loaded from JSON appear in the report. | `python -m pytest -q tests/test_report_evidence_links.py` |

> **Implementation specs:** every open gap below has a detailed, pick-up-cold task
> (context · steps · Definition-of-Done contract · verification) in
> [`docs/GAP_TASKS.md`](GAP_TASKS.md). Wave ordering & contracts: `docs/TASKS.md`.

## Undocumented gaps — re-audit 2026-06-24

Found by the `undocumented-gap-hunt` workflow (7-lens sweep + adversarial verification:
38 candidates → 19 confirmed real-and-undocumented, deduped and severity-ranked; 2
false-positives and 3 already-documented items dropped). Initially all `open`; GAP-012, GAP-020, and GAP-018 are now closed; the rest remain `open` unless noted. Full evidence/fix per
gap is in `.planning/coursework/research/bigdata/state-reaudit-tests-inventory-2026-06-23.md`
and the workflow transcript. GAP-012 was the most blocking item and is closed by
the 2026-06-24 regen-recipe fix and pipeline guard.

| ID | Sev | Area | Gap | Key evidence | Closure criterion |
|---|---|---|---|---|---|
| GAP-012 | high | docs | Closed 2026-06-24: the documented Bronze→Gold regen recipe now uses fresh `output/evidence/local-stats-bronze-regen`, avoiding the committed-manifest run-id collision, and the pipeline raises on a missing/empty local `--bronze-root` instead of writing empty Gold. | `bronze/live_check.py:645-654`; `pipeline.py` local-root guard | Clean-checkout regen passed with 2,139×3 Gold, AT/HU, WB `rail_network_length_km`, 1995-2021; deterministic integration tests cover missing-root failure and live-check nesting: `pytest -q -m integration`. |
| GAP-013 | medium | pipeline | **Closed 2026-06-24:** the live (s3/MinIO) branch of `_read_bronze_stats_frames` now reads World Bank JSON in addition to Eurostat via a new `_read_bronze_worldbank` reader (reusing the deterministic `silver.stats.load.load_worldbank_frame`, skipping `_`-prefixed catalogue datasets) and WARNs when zero World Bank frames are read; the local `bronze_root` branch is unchanged. | `pipeline.py:209-251`; `tests/test_pipeline_live_stats_worldbank.py` | A deterministic fsspec `memory://` integration test drives `_read_bronze_stats_frames` via a fake lander exposing `.s3` (no `bronze_root`) seeded with both `stats/worldbank/*.json` and `stats/eurostat/*.tsv`, asserts a `source_system='worldbank'` frame is returned alongside Eurostat with byte-exact WB values, and asserts the zero-WB WARN fires: `pytest -q -m integration tests/test_pipeline_live_stats_worldbank.py` passed (2 passed). |
| GAP-014 | medium | pipeline | **Closed 2026-06-24:** the s3/MinIO text branch now reads bytes and decodes UTF-8 explicitly, matching the local branch's UTF-8 contract for non-ASCII news payloads. | `pipeline.py:277-281`; `tests/test_pipeline_s3_readback.py` | `python -m pytest -q -m unit tests/test_pipeline_s3_readback.py` passed: 6 passed; UTF-8 memory-backed s3 read parity is asserted with accents and typographic quotes. |
| GAP-015 | medium | gold | Gold never normalizes units despite the documented Silver→Gold contract promising it; conflict resolution merges differently-scaled sources and the unit column is dropped on pivot. | `silver/config.py:25-26`, `gold/build.py` | A test feeds a worldbank-vs-eurostat collision on one feature with differently-scaled native values and asserts a single canonical-unit magnitude (or a quarantined conflict), and docs/code unit claims agree: `pytest -q -m unit`. |
| GAP-016 | medium | gold | Gold news feature schema is non-deterministic across runs: event-type and operator columns vary by batch because pivots are never reindexed to the canonical vocabularies. | `gold/build.py:99-117` | A test asserts `set(aggregate_news(A).columns)==set(aggregate_news(B).columns)` for disjoint-event/operator batches and that all `news_n_<event>`/`news_op_<op>` columns are present: `pytest -q -m unit`. |
| GAP-017 | medium | spark | **Closed 2026-06-24: adopted and pinned the Spark 4.1 stack.** `pyspark>=3.5` unbounded resolved to Spark 4.1.x; pinning the old Spark-3.5 stack is not viable on the repo's **Python 3.14 / pandas 3.0 / pyarrow 24** runtime. Spark 4.1 adds Python 3.14 support, Delta 4.1 supports Spark 4.1, and Hadoop S3A stays on the matching 3.4.1 generation. No live Spark run is claimed here; GAP-009 owns execution evidence. | `pyproject.toml:26-30`, `src/railway_lakehouse/spark_config.py`, `tests/test_spark_stack_pins.py` | `pyspark==4.1.*` + `delta-spark==4.1.*` are the only pip extra deps; S3A is recorded for `spark.jars.packages` as `org.apache.hadoop:hadoop-aws:3.4.1,software.amazon.awssdk:bundle:2.24.6`; **JDK 17 or 21** + `JAVA_HOME` documented. Verification command: `python -m pytest -q tests/test_spark_stack_pins.py`; dry-run install command: `python -m pip install --dry-run ".[spark]"`. |
| GAP-018 | medium | ops | Closed 2026-06-24: pandas/pyarrow majors are upper-bounded, `requires-python` is bounded to include 3.14, `constraints.txt` pins the graded runtime/test stack, and `tests/test_env_versions.py` guards against wrong-major drift. | `pyproject.toml`, `constraints.txt`, `tests/test_env_versions.py` | Closed: `python -m pytest -q tests/test_env_versions.py` passed with 5 tests; `python -m pytest -q` passed with 92 tests; constrained dry-run kept pandas 3.0.3 / pyarrow 24.0.0. |
| GAP-019 | medium | ops | **Closed 2026-06-24:** the Bronze scheduler now preflights storage (`_storage_reachable`), wraps each batch (`_run_batch`) so a MinIO-unreachable boot or a failing batch **degrades** (warns + writes a `status:"degraded"` manifest under `output/evidence/scheduler/`) instead of crashing, and makes the schedule loop body (`_tick`) exception-safe so one bad batch never kills cadence. A deployable host exists (Compose `scheduler` service, `depends_on: minio`, `restart: unless-stopped`, runs `bronze.run schedule`; minimal `Dockerfile`) plus a documented systemd-timer/cron runbook in `docs/OPERATIONS.md`/`README.md`. Bronze write semantics (lander) unchanged. | `src/railway_lakehouse/bronze/run.py`; `docker-compose.yml`; `Dockerfile`; `docs/OPERATIONS.md`; `tests/test_bronze_scheduler.py` | `python -m pytest -q -m unit tests/test_bronze_scheduler.py` passed (4 passed): degraded-on-exception writes a degraded manifest without raising, `_storage_reachable()` returns False on error, `_tick()` swallows a scheduler exception, success path writes an `ok` manifest. Full `pytest -q` → green; `compileall` clean. |
| GAP-020 | medium | tests | **Closed 2026-06-24:** deterministic unit coverage now drives `_list_bronze_files`, `_read_tsv`, and `_read_text` through an injected fsspec memory:// s3 backend, including gzip-over-s3, sidecar filtering, no-backend `ValueError`, and local/s3 read parity. | `pipeline.py:248-281`; `tests/test_pipeline_s3_readback.py` | `python -m pytest -q -m unit tests/test_pipeline_s3_readback.py` passed: 6 passed; `python -m pytest -q -m unit` passed: 83 passed, 10 deselected; `python -m pytest -q` passed: 93 passed. |
| GAP-021 | low | silver | The crosswalk cache treats `unmapped` as terminal with no rebuild path: labels cached unmapped (e.g. during an Ollama-down run) are never re-attempted; reruns replay the committed cache. | `silver/stats/merge.py:186-205` | A test caches a label `unmapped` with `use_llm=False`, reruns `build_crosswalk(use_llm=True)` with a stubbed LLM, and asserts the label now maps; a rebuild/no-cache flag forces recomputation: `pytest -q -m unit`. |
| GAP-022 | low | gold | Gold date parsing relies on pandas single-format inference (silent NaT on mixed formats once a non-pipeline Silver path feeds Gold); `_normalize_article_date` also silently drops RFC-822 RSS dates. | `gold/build.py:74`, `pipeline.py:342-357` | A test feeds mixed-format `published_date` (ISO, GDELT compact, RFC-822) through `aggregate_news`, asserts every valid-date row is counted and a warning logs any unparseable-but-present date: `pytest -q -m unit`. |
| GAP-023 | low | silver | Eurostat geo filtering is an implicit, unlogged side effect of the `[A-Z]{2}` regex: aggregate/multi-char geos (EU27_2020, EA19) are silently coerced to NaN and dropped at the Gold pivot. | `merge.py:66` | Eurostat geo selection uses an explicit allowed-geo set and logs a drop count for non-target/aggregate geos; a test asserts EU27_2020/EA19 are dropped via the explicit filter and counted: `pytest -q -m unit`. |
| GAP-024 | low | bronze | Bronze same-day re-pull overwrites the prior object and its `.meta.json` provenance despite the lander's "auditable accumulate, not overwrite" docstring; only the Silver contract was corrected for this. | `bronze/lander.py:56-66` | The Bronze section of DATA_CONTRACTS.md and the lander/rss docstrings state day-granularity replace (or the partition prefix carries run_id): manual review. |
| GAP-025 | low | silver | Silver `parse_rss_xml` calls `ET.fromstring` with no `ParseError` handling, so one malformed feed aborts the entire Silver news batch. | `silver/news/rss.py:8-9` | A test feeds a malformed RSS doc alongside a valid one and asserts the valid feed's articles return, the bad feed is skipped/logged, and no exception aborts the batch: `pytest -q -m unit`. |
| GAP-026 | low | gold | `aggregate_news` raises an opaque pandas `KeyError` on dict news rows that omit optional `NewsFeature` fields, despite the advertised dict-or-object input contract. | `gold/build.py:71,96,108` | A test passes a dict news row omitting `monetary_amount_eur`/`operators` to `aggregate_news` and asserts it aggregates without raising (defaults applied): `pytest -q -m unit`. |
| GAP-027 | low | ops | MinIO default credentials `admin`/`password123` are hardcoded in compose and config, and the service publishes both ports on all interfaces with no local-only security note. | `docker-compose.yml:20-21` | README documents the creds as insecure local-only defaults requiring `.env` override for shared hosts, and/or compose binds ports to `127.0.0.1`: manual review. |
| GAP-028 | low | ops | No CI runs the test suite; the only GitHub Actions workflow is the non-blocking dashboard-sync comment bot. | `.github/workflows/dashboard-sync-reminder.yml` | Either a CI workflow runs `pip install -e ".[test]" && pytest -q -m "unit or integration"` on PR/push (green run recorded), or the manual pytest step is added to the PR template and the decision is recorded here. |
| GAP-029 | low | docs | DATA_CONTRACTS.md Silver News field list omits `monetary_raw` (14 of 15 `NewsFeature` fields) while persisted Parquet has all 15. | `silver/schema.py:39-54` | DATA_CONTRACTS.md's Silver News list includes `monetary_raw` or explicitly states it is an illustrative subset with `schema.py` authoritative: manual review. |
| GAP-030 | low | docs | `pipeline.py` docstring says "Run inside your Docker stack … a running Ollama" but `docker-compose.yml` ships MinIO only. | `pipeline.py:10-13` | The module docstring no longer implies compose provides Ollama and matches the README's MinIO-only scope: manual review. |

## Test Failure Mapping

Update this section after each test run.

| Test / Command | Result | Gap ID | Notes |
|---|---|---|---|
| `python -m pip install --no-cache-dir -e ".[test]"` | passed | GAP-001 | First attempt failed due `[Errno 28] No space left on device`; pip cache was purged and final install passed with pinned S3 dependencies. |
| `python -m pytest -q -m unit` | passed: 15 passed, 1 deselected | GAP-008 | Bronze, Silver, and Gold characterization tests passed after migration. |
| `python -m pytest -q` | passed: 15 passed, 1 xfailed | GAP-004 | `test_pipeline_storage_read_stubs_are_not_wired` is a strict expected failure for the Bronze read stubs. |
| `python -c "import railway_lakehouse; import railway_lakehouse.pipeline"` | passed | GAP-002 | Package import root works after `src/railway_lakehouse` migration. |
| `python -m pip check` | passed | GAP-001 | No broken requirements found after dependency pinning. |
| Bounded live parser probe, manifest at `output/evidence/parser-live-check-2026-06-21/manifest.json` | partial | GAP-005 / GAP-010 | RSS and KSH landed raw artifacts; Eurostat/World Bank catalogues landed but need parser validation; GDELT hit 429; Statistics Austria seed was empty; UIC seed returned 404. |
| `python -m pytest -q tests\test_bronze_live_check.py` | passed: 3 passed | GAP-010 | Local Bronze lander, manifest writer, and bounded runner behavior are unit-tested without network or MinIO. |
| `python -m pytest -q` | passed: 18 passed, 1 xfailed | GAP-004 / GAP-010 | Full suite passed; existing `test_pipeline_storage_read_stubs_are_not_wired` remains the expected GAP-004 xfail. |
| `python -m railway_lakehouse.bronze.live_check --sources rss,ksh --out output/evidence/live-bronze --max-artifacts 5` | passed | GAP-010 | Manifest `output/evidence/live-bronze/manifest.json`; RSS passed with 5 artifacts, KSH passed with 3 artifacts, 264,670 total bytes, no source failures. |
| `python -m pytest -q tests\test_bronze_live_check.py` | passed: 8 passed | GAP-010 | PR #1 follow-up added rerun-safe output, preflight validation, collector response, and failure-exit coverage. |
| `python -m pytest -q tests\test_bronze_characterization.py` | passed: 11 passed | GAP-005 / GAP-010 | Eurostat quoted-code parsing and World Bank indicator validation are covered after PR #2 and PR #3. |
| Eurostat direct probe for `enpe_rail_go` | passed: HTTP 200, 552 bytes | GAP-010 | Bounded probe used `https://ec.europa.eu/eurostat/api/dissemination/sdmx/2.1/data/enpe_rail_go/?format=TSV&compressed=true`. |
| World Bank bounded direct probe | passed | GAP-010 | Accepted `IS.RRS.TOTL.KM`, `IS.RRS.GOOD.MT.K6`, and `IS.RRS.PASG.KM`; rejected `BM.GSR.TRAN.CD` as an API error envelope. |
| `python -m pytest -q` | passed: 29 passed, 1 xfailed | GAP-004 / GAP-010 | Final suite after merging PR #1, PR #2, and PR #3; the only expected failure is `test_pipeline_storage_read_stubs_are_not_wired` for GAP-004. |
| `python -m compileall .` | passed | GAP-001 / GAP-002 | Final syntax/import-bytecode sweep after all PR merges passed. |
| `python -m pytest -q tests\test_bronze_characterization.py` | passed: 15 passed | GAP-005 / GAP-010 | KSH curated seed regression tests and XLSX response validation now pass with existing Eurostat/World Bank Bronze characterization. |
| `python -m pytest -q tests\test_bronze_live_check.py` | passed: 8 passed | GAP-010 | Local KSH live-check collector handles `KshTable` seeds and rejects empty, malformed, or non-XLSX HTTP-200 bodies. |
| `python -m pytest -q tests\test_bronze_live_check_integration.py` | passed: 1 passed | GAP-010 | Integration fixture exercises `run_live_check`, the KSH collector, manifest writing, raw Bronze file writing, and metadata writing without network. |
| `python -m railway_lakehouse.bronze.live_check --sources ksh --out output/evidence/ksh-live-check-2026-06-22-current --max-artifacts 6 --timeout-seconds 30` | passed | GAP-010 | Current implementation landed 6 KSH artifacts, 92,509 bytes, 0 failures; committed manifest is `output/evidence/ksh-live-check-2026-06-22-current/manifest.json`. |
| `python -m pytest -q tests\test_bronze_live_check.py` | passed: 9 passed | GAP-010 | Local live-check tests now include UIC PDF response validation and manifest collection behavior. |
| UIC bounded direct probe | passed | GAP-010 | Current public UIC resource endpoints `help_resource/?id=12` and `help_resource/?id=14` returned HTTP 200 PDF bytes; stale `https://uic.org/IMG/xls/uic_railway_statistics_synopsis.xls` returned HTTP 404. |
| `python -m pytest -q tests\test_bronze_characterization.py::test_uic_public_resources_use_current_free_pdf_endpoints tests\test_bronze_characterization.py::test_uic_pdf_validation_rejects_html_empty_and_non_200 tests\test_bronze_characterization.py::test_uic_ingest_lands_valid_public_pdfs_and_skips_html_or_404 tests\test_bronze_live_check.py::test_collect_uic_lands_successes_and_records_failures` | passed: 4 passed | GAP-005 / GAP-010 | UIC source tests cover current public resource ids, PDF validation, ingest metadata, and live-check collection. |
| `python -m railway_lakehouse.bronze.live_check --sources uic --out output/evidence/uic-live-check-2026-06-22 --max-artifacts 2 --timeout-seconds 30` | passed | GAP-010 | Current implementation landed 2 UIC public PDF artifacts, 2,109,240 bytes, 0 failures; manifest is `output/evidence/uic-live-check-2026-06-22/manifest.json`. |
| `python -m pytest -q` | passed: 43 passed, 1 xfailed | GAP-004 / GAP-010 | Full suite passed after UIC refresh and concurrent GDELT history safety work; the only expected failure remains `test_pipeline_storage_read_stubs_are_not_wired` for GAP-004. |
| `python -m pytest -q tests\test_bronze_characterization.py -k "gdelt or past_recordings"` | passed: 6 passed, 17 deselected | GAP-010 | GDELT recent/history mocked 429 retry handling, `Retry-After`, 200-record DOC bound, and history `--dry-run` / `--max-pages` controls pass. |
| `python -m pytest -q tests\test_bronze_characterization.py` | passed: 23 passed | GAP-005 / GAP-010 | Bronze characterization now includes GDELT rate-limit tests plus existing Eurostat, World Bank, KSH, and UIC coverage. |
| Bounded GDELT recent live retry probe, manifest at `output/evidence/gdelt-live-check-2026-06-22/manifest.json` | failed | GAP-010 | One-day, `max_records=25`, `max_retries=2` probe landed 0 artifacts; HU returned HTTP 429 after retry handling and AT failed with a remote disconnect. Keep GDELT as non-blocking parser hardening unless the report needs GDELT-specific news evidence. |
| Parser scope decision | recorded | GAP-010 | Recent GDELT remains not working for live Bronze collection and is not a blocker for the Bronze MVP from proven sources. UIC public PDF collection is Bronze-complete; PDF fact extraction is Silver parser work. |
| `python -m pytest -q` | passed: 43 passed, 1 xfailed | GAP-004 / GAP-010 | Full suite passes; the only expected failure remains `test_pipeline_storage_read_stubs_are_not_wired` for GAP-004. |
| `python -m compileall src tests` | passed | GAP-001 / GAP-002 | Syntax/import-bytecode sweep over current source and tests passed. |
| `git diff --check` | passed | repo hygiene | No whitespace errors in the current working-tree diff. |
| `python -m pytest -q tests\test_pipeline_gaps.py` | passed: 3 passed | GAP-004 | Fixture-backed Bronze readers, zero-limit news behavior, and Bronze -> Silver -> Gold Parquet path are covered with local fixtures and mocked Ollama JSON output. |
| `python -m pytest -q -m integration` | passed: 4 passed, 52 deselected | GAP-004 / GAP-010 | Integration marker suite now has no GAP-004 xfail. |
| `python -m railway_lakehouse.pipeline --bronze-root tests\fixtures\bronze --news 1 --out output\evidence\fixture-e2e\railway_ml.parquet --crosswalk-path output\evidence\fixture-e2e\crosswalk_cache.json --skip-news-extraction` | passed: wrote 4 rows, 3 columns | GAP-004 / GAP-010 | Deterministic no-network fixture evidence written under `output/evidence/fixture-e2e/`; news extraction skipped to avoid live Ollama. |
| `python -m pytest -q` | passed: 56 passed | GAP-004 / GAP-010 | Full suite passes after closing the GAP-004 xfail. |
| `python -m compileall src tests` | passed | GAP-001 / GAP-002 | Syntax/import-bytecode sweep passed after GAP-004 changes. |
| `git diff --check` | passed | repo hygiene | Exited 0 after GAP-004 changes. |
| PR #8 review follow-up | passed | GAP-004 / repo hygiene | Addressed the other agent's minor nits: safer path errors, reproducible crosswalk evidence path, article date/body/id normalization, limit cleanup, and docstring wording. |
| `python -m pytest -q tests\test_pipeline_gaps.py` | passed: 5 passed | GAP-004 | Added review-follow-up coverage for crosswalk path CLI, article normalization, and malformed Bronze path errors. |
| `python -m pytest -q -m integration` | passed: 6 passed, 54 deselected | GAP-004 / GAP-010 | Integration marker suite includes the review-follow-up GAP-004 tests. |
| `python -m pytest -q` | passed: 60 passed | GAP-004 / GAP-010 | Full suite passes after PR #8 review-follow-up changes. |
| PR #9 and PR #10 local main sync | passed | GAP-006 | Merged Silver news parser and Silver stats fixture slices into local `main`; both PRs were previously GitHub `MERGEABLE` / `CLEAN`. |
| `python -m pytest -q` | passed: 74 passed | GAP-004 / GAP-006 / GAP-010 | Full suite passed after merging PR #9 and PR #10 into `main`. |
| `python -m compileall -q src tests` | passed | GAP-001 / GAP-002 | Syntax/import-bytecode sweep passed after merging PR #9 and PR #10. |
| `git diff --check` | passed | repo hygiene | Exited 0 after documentation sync and PR #9/#10 merge. |
| `python -m railway_lakehouse.bronze.live_check --sources eurostat,worldbank --out output/evidence/local-stats-bronze-regen --max-artifacts 1 --timeout-seconds 60` | passed | GAP-010 / GAP-012 | Corrected clean-checkout regen Bronze command landed 4 artifacts and 14,996,995 bytes directly under the fresh regen dir; raw Bronze bytes are ignored and must be regenerated. |
| `python -m railway_lakehouse.pipeline --bronze-root output/evidence/local-stats-bronze-regen/bronze --skip-news-extraction --news 0 --out output/evidence/local-stats-bronze-regen/railway_ml.parquet --crosswalk-path output/evidence/local-stats-bronze-regen/crosswalk_cache.json --counts-out output/evidence/local-stats-bronze-regen/counts.json` | passed | GAP-010 / GAP-012 | Corrected first-real Gold regen wrote 2,139 rows x 3 columns with World Bank `rail_network_length_km`, AT/HU present, and years 1995-2021; Eurostat raw landed but did not contribute a mapped Gold feature. |
| `python -m railway_lakehouse.pipeline --bronze-root output/evidence/does-not-exist/bronze --skip-news-extraction --news 0 --out output/runtime/gap-012-negative/empty.parquet` | failed as expected | GAP-012 | Missing `--bronze-root` now exits non-zero with a `FileNotFoundError` mentioning the path and `live_check`; no empty parquet was written. |
| `python -m pytest -q` after rebasing PR #12 on merged PR #11 | passed: 83 passed | GAP-006 / GAP-010 | Combined Silver persistence plus local stats Bronze -> Gold branch is green before merge. |
| `python scripts/minio_smoke.py` | passed | GAP-010 | Local MinIO smoke wrote/read/deleted `bronze/_smoke/hello.txt` and recorded `output/evidence/minio-smoke/manifest.json` with `roundtrip_ok=true`, buckets `bronze`, `silver`, and `gold`, and 32 bytes written/read. |
| `python -m pytest -q tests/test_infra_minio.py` | passed: 4 passed | GAP-010 | Deterministic MinIO infra guard checks committed defaults, `.env` loading for the smoke script, Compose bucket bootstrap config, and smoke evidence wiring without Docker. |
| `python -m pytest -q` (2026-06-24 re-audit) | passed: 87 passed | GAP-008 | Full suite green on Python 3.14.0 / pandas 3.0.3 / pyarrow 24.0.0 / s3fs 2024.6.1; editable install, no PYTHONPATH needed. |
| `python -m pytest -q -m unit` / `-m integration` (2026-06-24) | passed: 77 / 10 | GAP-004 / GAP-008 | Marker split is 77 unit + 10 integration = 87. `-m live` and `-m spark` select 0 (no such pytest tests exist). |
| `docker compose up -d; python scripts/minio_smoke.py` (2026-06-24) | passed: LIVE | GAP-010 | First live MinIO run this machine (prior session's Docker engine was down). `railway-minio` up on 9000/9001; compose `createbuckets` created bronze/silver/gold via `mc`; s3fs 32 B write/read round-trip on the **bronze** bucket, `roundtrip_ok=true`. |
| `python -m railway_lakehouse.bronze.live_check --sources eurostat,worldbank --out output/evidence/inventory-live-2026-06-23 --max-artifacts 2` (2026-06-24) | partial | GAP-010 | World Bank PASSED: 3 artifacts (IS.RRS.TOTL.KM, IS.RRS.GOOD.MT.K6, catalogue), ~17 MB. Eurostat FAILED on a transient `RemoteDisconnected` on the catalogue TOC fetch (network flake; succeeded in prior committed evidence). |
| `python -m railway_lakehouse.pipeline --bronze-root output/evidence/inventory-live-2026-06-23/bronze --skip-news-extraction --news 0 --out …/railway_ml.parquet --counts-out …/counts.json` (2026-06-24) | passed | GAP-010 | Live WB Bronze→Silver→Gold: Gold **2,968 rows × 4 cols** `[geo, year, rail_freight_tonne_km, rail_network_length_km]`, 151 geos, 1995–2021, AT/HU 27 rows each; crosswalk 2/2 mapped, merge kept 35112/35112. Supersedes the prior 2,139×3 single-feature smoke. AT 1995 freight=13715, network=5672. |
| Silver persist round-trip + MinIO upload (2026-06-24) | passed | GAP-006 / GAP-010 | 35,112 StatFact rows persisted via `silver/persist.py` and reloaded identically; the parquet was uploaded to the MinIO `silver` bucket (`silver/stats/stat_fact/ingest_date=2026-06-23/stat_fact.parquet`, 35,961 B) as a manual demonstration that the lakehouse holds Silver objects. |
| `python -m compileall -q src tests` (2026-06-24) | passed | GAP-001 / GAP-002 | Syntax/import sweep clean on the re-audit. |
| `python -m pytest -q -m integration` (GAP-012) | passed: 13 passed, 77 deselected | GAP-012 | Includes the new missing-root guard and live-check nesting contract tests. |
| `python -m pytest -q -m unit` (GAP-012) | passed: 77 passed, 13 deselected | GAP-012 | Existing `tests/test_bronze_live_check.py::test_run_live_check_uses_run_subdirectory_when_output_already_has_evidence` also passed independently. |
| `python -m pytest -q` (GAP-012) | passed: 90 passed | GAP-012 / GAP-008 | Full suite green after the pipeline guard and docs sync. |
| `python -m compileall -q src tests` (GAP-012) | passed | GAP-012 | Syntax/import sweep clean after the pipeline guard and tests. |
| `git diff --check` (GAP-012) | passed | repo hygiene | Whitespace check exited 0 after GAP-012 changes. |
| `python -m pytest -q -m unit tests/test_pipeline_s3_readback.py` | passed: 6 passed | GAP-020 / GAP-014 | Deterministic fsspec memory:// coverage drives s3 glob/filtering, no-backend `ValueError`, gzip TSV read-back, TSV local/s3 parity, news local/s3 article parity, and UTF-8 text round-trip after the binary UTF-8 decode fix. |
| `python -m pytest -q -m unit` (2026-06-24 GAP-020) | passed: 83 passed, 10 deselected | GAP-020 / GAP-014 | Unit marker count increased from 77 to 83 with the six new s3/MinIO Bronze read-back tests. |
| `python -m pytest -q` (2026-06-24 GAP-020) | passed: 93 passed | GAP-020 / GAP-014 | Full suite stayed green after the GAP-020 tests and GAP-014 `_read_text` UTF-8 fix. |
| `python -m pytest -q tests/test_env_versions.py` (GAP-018) | passed: 5 passed | GAP-018 | Guards installed pandas/pyarrow windows, cross-checks pyproject bounds with `tomllib`, verifies `constraints.txt` pins pandas 3.0.3 and pyarrow 24.0.0, and includes permanent wrong-major negative assertions. |
| `python -m pytest -q` (GAP-018) | passed: 92 passed | GAP-018 / GAP-008 | Full deterministic suite remains green after adding the environment-version guard. |
| `python -m pip install --dry-run -e ".[test]" -c constraints.txt` | passed | GAP-018 | Dry-run reported pandas 3.0.3 satisfying `pandas<4,>=2.2` and pyarrow 24.0.0 satisfying `pyarrow<25,>=15`; no future major selected. |
| `python -m pytest -q -m integration` (GAP-007) | passed: 14 passed, 89 deselected | GAP-007 | Includes `tests/test_gold_load_from_silver.py`, which persists fixture-derived Silver to `tmp_path`, invokes `gold.run.main([...])`, and verifies Gold Parquet plus counts JSON with AT/HU and `rail_passenger_km`. |
| `python -m pytest -q` (GAP-007) | passed: 103 passed | GAP-007 / GAP-008 | Full deterministic suite stayed green after wiring `gold.run` to persisted Silver and moving `write_gold_counts` into `gold.build`. |
| `python -m railway_lakehouse.gold.run --silver-root output/runtime/gap-007-cli-smoke/silver --out output/runtime/gap-007-cli-smoke/gold/railway_ml.parquet --counts-out output/runtime/gap-007-cli-smoke/gold/counts.json --ingest-date 2026-06-23` | passed | GAP-007 | Local CLI smoke read persisted Silver Parquet, wrote Gold 4 rows x 4 columns, and recorded counts with `contains_AT=true`, `contains_HU=true`, `year_min=2020`, `year_max=2021`, and `rail_passenger_km`. |
| `python -m pytest -q -m "unit or integration"` (GAP-009) | passed: 103 passed, 2 deselected | GAP-009 / GAP-017 | CI-safe guard path passes; `tests/test_spark_stack_pins.py` imports `railway_lakehouse.spark_jobs.coverage` without importing PySpark and verifies the pinned Spark extra/S3A Maven coordinates. |
| `python -m pytest -q -m spark` (GAP-009) | passed: 2 passed, 103 deselected | GAP-009 | Spark-marked tests build Gold inputs under `tmp_path`, assert manifest/output files, and cover missing-input plus 0-row loud failures. Runtime env: PySpark 4.1.2, JDK 21.0.11, `HADOOP_HOME=tmp/hadoop-3.4.0-win10-x64` for native Windows local writes. |
| `python -m railway_lakehouse.spark_jobs.coverage --input output/evidence/inventory-live-2026-06-23/railway_ml.parquet --out output/evidence/spark/` | passed | GAP-009 | Spark evidence manifest records Spark 4.1.2, JDK 21.0.11, input 2,968 rows x 4 columns, output 2,968 rows x 5 columns, one part-file, `_SUCCESS`, UTC timestamps, and `status=passed`. |
| `python -m pytest -q` (GAP-009) | passed: 105 passed | GAP-009 / GAP-008 | Full suite green with the Spark runtime env after adding the Spark evidence job and tests. |
| `python -m compileall -q src tests` (GAP-009) | passed | GAP-009 / GAP-002 | Syntax/import-bytecode sweep clean after the Spark job package and tests. |
| `python -m pytest -q tests/test_report_evidence_links.py` (GAP-011) | passed: 3 passed | GAP-011 | Evidence-link checker verifies report/presentation existence, all cited `output/evidence/` paths, and headline values loaded from committed JSON. |
| `python -m pytest -q` with `JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot` (GAP-011) | passed: 108 passed, 1 skipped | GAP-011 / GAP-008 | Full suite green after report/presentation drafts. The skip is the existing Windows Spark write-path skip because `HADOOP_HOME`/`winutils.exe` is absent in this worktree. |
| `python -m compileall -q src tests` (GAP-011) | passed | GAP-011 / GAP-002 | Syntax/import-bytecode sweep clean after adding the report evidence checker. |
| `python -m pytest -q -m integration tests/test_pipeline_live_stats_worldbank.py` (GAP-013) | passed: 2 passed | GAP-013 | Live (s3) `_read_bronze_stats_frames` returns a `source_system='worldbank'` frame alongside Eurostat from an fsspec `memory://` seed with both sources; WB values byte-exact (HU=789.12, AT=456.78); `_catalogue` dataset skipped; zero-WB seed logs the WARN. No Docker/MinIO/network. |
| `python -m pytest -q` with `JAVA_HOME=...jdk-21.0.11.10-hotspot` (GAP-013) | passed: 123 passed, 1 skipped | GAP-013 / GAP-008 | Full suite green after wiring live World Bank into the stats read path; the skip is the existing Windows Spark write-path skip (`HADOOP_HOME`/`winutils.exe` absent). |
| `python -m compileall -q src tests` (GAP-013) | passed | GAP-013 / GAP-002 | Syntax/import-bytecode sweep clean after the live World Bank reader. |
| `python -m pytest -q -m unit tests/test_bronze_scheduler.py` (GAP-019) | passed: 4 passed | GAP-019 | Degraded-on-exception batch writes a `status:"degraded"`/`storage_reachable:false` manifest without raising; `_storage_reachable()` returns False when `exists` raises; `_tick()` swallows a scheduler exception (returns False); success path writes an `ok` manifest. Deterministic — monkeypatch + injected clock + tmp_path; no Docker/network. |
| `python -m pytest -q` with `JAVA_HOME=...jdk-21.0.11.10-hotspot` (GAP-019) | passed: 127 passed, 1 skipped | GAP-019 / GAP-008 | Full suite green after the scheduler hardening (rebased on GAP-013's 2 new tests → 125+2=127); the skip is the existing Windows Spark write-path skip. |
| `python -m compileall -q src tests` (GAP-019) | passed | GAP-019 / GAP-002 | Syntax/import-bytecode sweep clean after the deployable scheduler changes. |
